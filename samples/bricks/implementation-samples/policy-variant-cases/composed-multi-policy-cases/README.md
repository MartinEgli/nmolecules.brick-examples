# Composed Multi-Policy Cases

This project demonstrates policy composition across platform, product, team and experimental policies.

It covers composition modes such as import, narrow, override and disable, then evaluates the composed policy against dependencies.

Use this sample when teams need layered ownership of architecture rules.
