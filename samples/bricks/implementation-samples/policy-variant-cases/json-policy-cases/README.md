# JSON Policy Cases

This project demonstrates policy loading from JSON.

The embedded policy document includes:

- rules
- imports
- aliases
- external role assignments
- namespace and assembly dependency scopes

Use this sample when policy should be authored outside compiled source code.
